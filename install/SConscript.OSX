# SCons declarative input file that controls how Slither OS X dmg (disk image)
#  is built...

# Imported libraries...
import os, shutil, sys, posix, platform

# Import the environment of our parent SConstruct...
Import('env')

# Stubbed...
print "OS X disk image target stubbed..."


